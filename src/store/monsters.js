const state = {
  monsters: ["Barakka", "SubZero"],
};

const mutations = {};

const actions = {};

const getters = { selectedMonsters: (state) => state };

export default {
  state,
  getters,
  actions,
  mutations,
};
